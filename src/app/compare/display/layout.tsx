import React from "react";

export const metadata = {
  title: "Display comparison",
};

export default function CompareDisplayLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
